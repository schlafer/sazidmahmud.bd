<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Tools on Sazid Mahmud</title>
    <link>https://sazidmahmud.bd/tags/tools/</link>
    <description>Recent content in Tools on Sazid Mahmud</description>
    <generator>Hugo -- 0.164.0</generator>
    <language>en</language>
    <lastBuildDate>Tue, 28 Oct 2025 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://sazidmahmud.bd/tags/tools/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Mise-en-place</title>
      <link>https://sazidmahmud.bd/posts/2-mise-en-place/</link>
      <pubDate>Tue, 28 Oct 2025 00:00:00 +0000</pubDate>
      <guid>https://sazidmahmud.bd/posts/2-mise-en-place/</guid>
      <description>&lt;h3 id=&#34;mise-en-place-meez-ahn-plahs---mise&#34;&gt;Mise-en-place (MEEZ ahn plahs) - &lt;code&gt;mise&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;The idea behind using it is to standardize development environment for everyone working in the team. Furthermore, we need different versions of tools with different projects. Mise takes care of that.&lt;/p&gt;
&lt;p&gt;Mise is a unified tool and task management solution that simplifies the process of installing and managing tools for different projects. It supports various backends for tool installation and allows users to define tasks with custom scripts.&lt;/p&gt;
&lt;p&gt;It can be used in both local development environments and CI/CD pipelines, serving as a single source of truth for tool versions and task flows.&lt;/p&gt;
&lt;p&gt;It serves as an excellent alternative to &lt;code&gt;make&lt;/code&gt;, &lt;code&gt;tfenv&lt;/code&gt;, &lt;code&gt;direnv&lt;/code&gt;, and whatever-else-env.&lt;/p&gt;
&lt;p&gt;Tired of writing boilerplate code to install necessary tools for pipelines? With &lt;code&gt;.mise.toml&lt;/code&gt;, why not use &lt;code&gt;mise install&lt;/code&gt; in pipelines too? Install mise in advance or use the Docker image &lt;code&gt;jdxcode/mise&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;There’s no need to track tool versions separately for development environments and CI anymore. You no longer need to remember to update tool versions in different places to prevent any issues. Mise serves as a single source of truth.&lt;/p&gt;
&lt;p&gt;If you use a similar task flow in CI as you do locally, you can run the same mise tasks there too. If not, you can use &lt;a href=&#34;https://mise.jdx.dev/profiles.html&#34;&gt;mise profiles&lt;/a&gt; to define CI tasks. Bonus: you can easily use it locally to debug pipeline issues.&lt;/p&gt;
&lt;p&gt;See the &lt;a href=&#34;https://mise.jdx.dev/tips-and-tricks.html#ci-cd&#34;&gt;docs&lt;/a&gt; for a GitHub Actions example.&lt;/p&gt;
&lt;h6 id=&#34;some-tips&#34;&gt;Some tips&lt;/h6&gt;
&lt;ol&gt;
&lt;li&gt;Place &lt;code&gt;.mise.toml&lt;/code&gt; in the root of your Git repository to define tools and tasks for the entire repository.&lt;/li&gt;
&lt;li&gt;If a repository contains several different projects (like a monorepo), keep common items (like pre-commit tools) in the root config and project-specific items in the project directories.&lt;/li&gt;
&lt;li&gt;Use your own &lt;code&gt;~/.config/mise/config.toml&lt;/code&gt; for personal automation tasks.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;a href=&#34;https://mise.jdx.dev/&#34;&gt;Try it!&lt;/a&gt;&lt;/p&gt;
</description>
      <content:encoded><![CDATA[<h3 id="mise-en-place-meez-ahn-plahs---mise">Mise-en-place (MEEZ ahn plahs) - <code>mise</code></h3>
<p>The idea behind using it is to standardize development environment for everyone working in the team. Furthermore, we need different versions of tools with different projects. Mise takes care of that.</p>
<p>Mise is a unified tool and task management solution that simplifies the process of installing and managing tools for different projects. It supports various backends for tool installation and allows users to define tasks with custom scripts.</p>
<p>It can be used in both local development environments and CI/CD pipelines, serving as a single source of truth for tool versions and task flows.</p>
<p>It serves as an excellent alternative to <code>make</code>, <code>tfenv</code>, <code>direnv</code>, and whatever-else-env.</p>
<p>Tired of writing boilerplate code to install necessary tools for pipelines? With <code>.mise.toml</code>, why not use <code>mise install</code> in pipelines too? Install mise in advance or use the Docker image <code>jdxcode/mise</code>.</p>
<p>There’s no need to track tool versions separately for development environments and CI anymore. You no longer need to remember to update tool versions in different places to prevent any issues. Mise serves as a single source of truth.</p>
<p>If you use a similar task flow in CI as you do locally, you can run the same mise tasks there too. If not, you can use <a href="https://mise.jdx.dev/profiles.html">mise profiles</a> to define CI tasks. Bonus: you can easily use it locally to debug pipeline issues.</p>
<p>See the <a href="https://mise.jdx.dev/tips-and-tricks.html#ci-cd">docs</a> for a GitHub Actions example.</p>
<h6 id="some-tips">Some tips</h6>
<ol>
<li>Place <code>.mise.toml</code> in the root of your Git repository to define tools and tasks for the entire repository.</li>
<li>If a repository contains several different projects (like a monorepo), keep common items (like pre-commit tools) in the root config and project-specific items in the project directories.</li>
<li>Use your own <code>~/.config/mise/config.toml</code> for personal automation tasks.</li>
</ol>
<p><a href="https://mise.jdx.dev/">Try it!</a></p>
]]></content:encoded>
    </item>
    <item>
      <title>Zoxide, the navigation solution</title>
      <link>https://sazidmahmud.bd/posts/1-zoxide-the-navigation-solution/</link>
      <pubDate>Thu, 16 Oct 2025 00:00:00 +0000</pubDate>
      <guid>https://sazidmahmud.bd/posts/1-zoxide-the-navigation-solution/</guid>
      <description>&lt;h3 id=&#34;zoxide---z&#34;&gt;Zoxide - &lt;code&gt;z&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;This has completely replaced &lt;code&gt;cd&lt;/code&gt; command for me.&lt;/p&gt;
&lt;p&gt;It&amp;rsquo;s fast and remembers your previous visit to directories, ranking them by visit frequency.&lt;/p&gt;
&lt;p&gt;You can jump to any previously visited directory without explicitly specifying the path to it like &lt;code&gt;z dir&lt;/code&gt;, where directory can look something like /home/user/&amp;hellip;./dir.&lt;/p&gt;
&lt;p&gt;If you have jumped enough times you don&amp;rsquo;t even need to write the full directory name.&lt;/p&gt;
&lt;p&gt;There is also an interactive selection using &lt;code&gt;fzf&lt;/code&gt;, so you can filter out similar named directories, &lt;code&gt;zi app&lt;/code&gt; will list out all the projects that include an &amp;ldquo;/app&amp;rdquo; directory.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z foo&lt;/code&gt; - cd into highest ranked directory matching foo.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z foo bar&lt;/code&gt; - cd into highest ranked directory matching foo and bar.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z foo /&lt;/code&gt; - cd into a subdirectory starting with foo.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z ~/foo&lt;/code&gt; - z also works like a regular cd command.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z foo/&lt;/code&gt; - cd into relative path.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z ..&lt;/code&gt; - cd one level up.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z -&lt;/code&gt; - cd into previous directory.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;zi foo&lt;/code&gt; - cd with interactive selection (using fzf).&lt;/p&gt;
&lt;p&gt;&lt;code&gt;z foo&amp;lt;SPACE&amp;gt;&amp;lt;TAB&amp;gt;&lt;/code&gt; - show interactive completions (zoxide v0.8.0+, bash 4.4+/fish/zsh only).&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/ajeetdsouza/zoxide&#34;&gt;Try it!&lt;/a&gt;&lt;/p&gt;
</description>
      <content:encoded><![CDATA[<h3 id="zoxide---z">Zoxide - <code>z</code></h3>
<p>This has completely replaced <code>cd</code> command for me.</p>
<p>It&rsquo;s fast and remembers your previous visit to directories, ranking them by visit frequency.</p>
<p>You can jump to any previously visited directory without explicitly specifying the path to it like <code>z dir</code>, where directory can look something like /home/user/&hellip;./dir.</p>
<p>If you have jumped enough times you don&rsquo;t even need to write the full directory name.</p>
<p>There is also an interactive selection using <code>fzf</code>, so you can filter out similar named directories, <code>zi app</code> will list out all the projects that include an &ldquo;/app&rdquo; directory.</p>
<p><code>z foo</code> - cd into highest ranked directory matching foo.</p>
<p><code>z foo bar</code> - cd into highest ranked directory matching foo and bar.</p>
<p><code>z foo /</code> - cd into a subdirectory starting with foo.</p>
<p><code>z ~/foo</code> - z also works like a regular cd command.</p>
<p><code>z foo/</code> - cd into relative path.</p>
<p><code>z ..</code> - cd one level up.</p>
<p><code>z -</code> - cd into previous directory.</p>
<p><code>zi foo</code> - cd with interactive selection (using fzf).</p>
<p><code>z foo&lt;SPACE&gt;&lt;TAB&gt;</code> - show interactive completions (zoxide v0.8.0+, bash 4.4+/fish/zsh only).</p>
<p><a href="https://github.com/ajeetdsouza/zoxide">Try it!</a></p>
]]></content:encoded>
    </item>
  </channel>
</rss>
